CC=g++ -Wall -Wextra -O3
CFLAGS= -Wl,--no-as-needed,-lTMVA
LDFLAGS=`root-config --glibs --cflags`
SOURCES= fillComparison.cc ../eventselection/src/eventSelections.cc ../eventselection/src/eventFlattening.cc ../eventselection/src/eventSelectionsParticleLevel.cc ../eventselection/src/eventFlatteningParticleLevel.cc ../../codeLibrary.o 
OBJECTS=$(SOURCES:.cc=.o)
EXECUTABLE=fillComparison

all: 
	$(CC) $(CFLAGS) $(SOURCES) $(LDFLAGS) -o $(EXECUTABLE)
        
clean:
	rm -rf *o $(EXECUTABLE)
